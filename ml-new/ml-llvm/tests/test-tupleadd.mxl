int[3] addTuples(int[3] x, int[3] y) {
	int i;
	int[3] t;

	for (i = 0; i < 3; i = i + 1) {
		t[i] = x[i] + y[i];
	}

	return t;
}

// ALEX -- this is what wee need to work on
int addTuples2(int[] x, int[] y) {
	return 3;
}

int main() {
	int[3] a;
	int[3] b;
	int[3] c;
	int i;

	a = [1, 2, 3];
	b = [1, 2, 3];

	c = addTuples(a, b);

	for (i = 0; i < c.length; i = i + 1) {
		print(c[i]);
	}

	return 0;
}
