
#include <stdlib.mxl>;

int main(){

    int[3][4:3] a;
    int[3][3:4] b;

    int x;
    int k;
    int l;
    int f;
    int[][][] mat;

    a = [|(3,2,3), (4,5,6), (8,5,6)| (4,3,2),(9,8,7),(3,1,3)|(3,2,1),(4,3,2),(2,1,1)|(6,7,5),(3,2,1),(7,8,9)|];
    
    rotateIntTupMatrixRight(@@@a, @@@b, a.rows, a.columns, a.length);
    mat = @@@b;
    for(x = 0; x<b.rows; x = x+1){
        for( k = 0; k<b.columns; k=k+1){
            for(l=0; l<b.length; l = l+1){
                f = $mat;
                printsl(f);
                mat = mat.+;
            }
        printssl("  ");
        }
    prints(" ");
    }      
    return 0;
}
