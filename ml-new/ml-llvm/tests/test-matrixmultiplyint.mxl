#include <stdlib.mxl>;
int main(){
    int[3:3] a; 
    int[3:3] b;

  int el;
  int el2;
  int i;
  int j;
  a = [| 1, 2, 3 | 1, 2, 3 | 2, 2, 2 |];
  b = [| 1, 2, 3 | 1, 2, 3 | 2, 2, 2 |];
    multiplyIntMatrices(@@a, @@b, a.rows, a.columns);
    /*
    for(i = 0; i<a.rows; i = i+1){
        for (j = 0; j<a.columns; j = j+1){
            el = a[i:j];
            print(el);
        }
    }
    */
    printIntMatrices(@@a, a.rows, a.columns);
    return 0;
}
