void printIntTuples(int[] x,  int len) {
         int i;
         int f;
         for (i = 0; i < len; i = i + 1) {
                 f = $x;
		 print(f);
		 x = x.+;
         }
}

int main(){
	int[5] tup;
	tup = [1,2,3,4,5];
	
	printIntTuples(@tup, tup.length);

	return 0;
}
